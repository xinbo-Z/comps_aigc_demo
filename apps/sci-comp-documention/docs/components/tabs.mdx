import PreviewBlock from '../../doc-components/PreviewBlock'
import { EditableTabsDocDemo, LazyTabsDocDemo, TabsDocDemo } from '../../doc-components/demos'

# Tabs

`Tabs` 是基于 Ant Design Tabs 的通用封装组件，当前保留 antd 原生标签页能力，并额外提供 `lazy` 语义化扩展。

## 实时预览

<PreviewBlock
  title="基础标签页"
  description="基础场景保留 antd tabs 的标签切换能力，适合模块化信息展示。"
>
  <TabsDocDemo />
</PreviewBlock>

## 使用示例

```tsx
import { Tabs } from '@sci-comp/core'

const items = [
  { key: 'overview', label: '概览', children: <div>概览内容</div> },
  { key: 'detail', label: '详情', children: <div>详情内容</div> },
]

export function Example() {
  return <Tabs items={items} defaultActiveKey="overview" />
}
```

## Lazy 实时预览

<PreviewBlock
  title="Lazy 渲染"
  description="开启 lazy 后，非激活标签页内容会延后到切换时再渲染。"
>
  <LazyTabsDocDemo />
</PreviewBlock>

## Lazy 示例

```tsx
import { Tabs } from '@sci-comp/core'

const items = [
  { key: 'overview', label: '概览', children: <div>概览内容</div> },
  { key: 'detail', label: '详情', children: <div>详情内容</div> },
]

export function LazyExample() {
  return <Tabs items={items} defaultActiveKey="overview" lazy />
}
```

## Editable 实时预览

<PreviewBlock
  title="可编辑标签页"
  description="editable-card 模式下可动态新增与关闭标签页。"
>
  <EditableTabsDocDemo />
</PreviewBlock>

## Editable 示例

```tsx
import { Tabs } from '@sci-comp/core'

const items = [
  { key: 'overview', label: '概览', children: <div>概览内容</div>, closable: true },
  { key: 'detail', label: '详情', children: <div>详情内容</div>, closable: true },
]

export function EditableExample() {
  return <Tabs items={items} type="editable-card" onEdit={() => {}} />
}
```
