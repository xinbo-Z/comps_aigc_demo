## 1. 中文化规则与范围确认

- [x] 1.1 盘点 `openspec/changes/`、`openspec/specs/` 与 `openspec/changes/archive/` 下需要中文化的 Markdown 文件范围
- [x] 1.2 明确保留英文结构关键字的规则，包括 Requirement、Scenario、WHEN/THEN 与增量操作标题

## 2. 活跃与主规范内容中文化

- [x] 2.1 将活跃 changes 中的 proposal、design、tasks 与 delta specs 正文统一转换为中文表达
- [x] 2.2 将 `openspec/specs/` 下主 specs 的说明文字、需求描述与场景正文统一转换为中文表达
- [x] 2.3 校验 `component-doc-standardization`、`progress-docs` 与新增 `openspec-chinese-localization` 相关内容符合中文化规则

## 3. 归档内容与流程验证

- [x] 3.1 将 `openspec/changes/archive/` 下仍需保留语义一致性的相关 OpenSpec 正文转换为中文表达
- [x] 3.2 运行 OpenSpec 相关状态检查，验证中文化后的内容不破坏 status、apply、sync 或 archive 流程
- [x] 3.3 复查翻译后内容是否仍保持规范结构、术语一致性与可读性
